# mwanachama-backend-taskmanager (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| Task | Title | Status | Notes |
|------|--------|--------|-------|
| W23 | 🐞 **SECURITY — `TraverseRelationships` has no page cap, unlike every other List* method — a single cheap `taskmanager_traverse_relationships` MCP call can force an unbounded row scan/return.** `list_limit.go`'s own doc comment states `maxListPage` (500) "caps every List* method reachable through the /mcp surface" — and every List* method in this package (`ListTasks`, `ListAgents`, `ListProjects`, `ListTasksInProject` via `ListTasks`, `ListTaskTodos`, `ListDeliverablesForTask`, `ListAcceptanceCriteriaForTask`, `ListWorkflowRuns`) does apply `.Limit(maxListPage)`. `TraverseRelationships` (`relationship_impl.go:268`, exposed as `mwanachama-backend-api-shared`'s `taskmanager_traverse_relationships` tool) is the one exception: its `traverseFK` "vertexID is the value every matching row's fk column points at" branch (line ~311, e.g. `assigned_to`/`has_todo` inbound) and its `traverseJoin` helper (line ~330, `blocks`/`depends_on`/`member_of`/`has_tag`, either direction) both do a plain `Pluck` with no `.Limit()` at all. **Did**: seeded 600 Tasks all `member_of` one Project via the real `TaskManager.CreateRelationship` (well within a normal caller's reach — no privilege needed), then called `TraverseRelationships(ctx, projectID, "member_of", DirectionInbound)` — the exact operation `taskmanager_traverse_relationships(vertex_id=projectID, label="member_of", direction="inbound")` performs over MCP. **Observed**: all 600 relationships returned in one call, no truncation, no error — confirmed by a throwaway test (`go test -run TestScratch_TraverseRelationships_Unbounded`, deleted before commit, not left in the tree). **Threat**: `mwanachama-backend-api-kazi` mounts this tool at `/mcp` behind `requireCapabilityStub` (an intentional, tracked allow-everyone placeholder pending K7/K8's per-agent auth — not itself refiled here) — so today, any caller who can reach that endpoint can force this unbounded scan/serialize/return on any vertex with a large fan-out (a busy Project, a heavily-blocked Task, a Tag applied broadly), at effectively zero cost to the caller and unbounded cost to the server, repeatable with no rate limit anywhere in the path. Also confirmed reachable through `mwanachama-backend-api-gateway`'s own `/mcp` (`internal/api/http/mcp.go`'s `newMCPServer` wires the same `apishared.MCPDeps{TaskManager: d.TaskManager, Git: d.Git, ...}` and calls `RegisterTools`) — there it sits behind real session auth (`d.auth`) plus a per-call `d.requireCapability`/`taskmanager.read` grant, not `requireCapabilityStub`, so it is not anonymous-reachable there; but any ordinary caller already holding that one read capability (not an elevated one) can still trigger the same unbounded internal scan. `mwanachama-wakala-api` was checked and ruled out — it imports neither `mwanachama-backend-taskmanager` nor `mwanachama-backend-git` at all (its domain is Agency, not task/git), so this shape doesn't apply there. **Control**: none — no `.Limit()`, no result-size ceiling, nothing analogous to `ListMergeRequests`/`ListKeywords`'s explicit `Limit` filter field either (the tool's own params carry no `limit`/pagination field at all). **Verdict**: real, reproducible gap in this repo's own stated invariant; not filed as a permanent self-test anywhere (fixing it is source-level, not something a self-test should paper over) — needs a `.Limit(maxListPage)` on both the `traverseFK` "many rows" branch and `traverseJoin`, matching every sibling List* method's own shape, ideally with the same `Limit` opt-in field `ListMergeRequests`/`ListKeywords` already expose for a caller that legitimately needs more than one page. | 📋 | Found 2026-09-21 by the scheduled cross-repo security QA sweep (primary this run: `mwanachama-backend-api-kazi`, oldest `cmd/server/selftest/security` commit of the three MCP-facing repos in rotation) while probing api-kazi's Cost/Availability surface per that run's own applicability note ("an unbounded fan-out tool — e.g. listing every task/relationship with no page limit"). Filed on this repo's board rather than api-kazi's or api-shared's, since the missing cap is in this repo's own `relationship_impl.go`, not the MCP wrapper (`mcp_tools_taskmanager.go`'s `mcpTraverseRelationships` just forwards `vertex_id`/`label`/`direction`, doing nothing wrong itself) or api-kazi's capability layer. No product code touched; the reproduction test file was deleted after confirming the result, per the sweep's own rule against leaving a probe file behind uncommitted. `mwanachama-backend-git`'s equivalent surface (`git_get_neighborhood`) was checked in the same pass and is NOT affected — `GetNeighborhood` already clamps `depth` and caps node count (`neighborhoodMaxNodes`, tested by `TestGetNeighborhood_DepthClamp`/`TestGetNeighborhood_NodeCapEnforced`), and every other git List* method applies `maxListPage` identically to this repo's own convention. |
| W22 | 🐞 **BUG — `NextCode` (`gormstore/codesequence.go`) reads the counter row then writes `next_number+1` with no CAS guard and no row lock, so two ordinary concurrent `CreateTaskTodo` calls (or `upsertTagByName` calls minting two distinct new tag names) can fail outright with a raw, unclassified database error.** `NextCode` does `SELECT ... WHERE entity_type = ?` (no `FOR UPDATE`/lock) to read `n`, computes `n+1` in Go, then `UPDATE ... SET next_number = <literal n+1> WHERE entity_type = ?` — no `WHERE next_number = ?` precondition, so nothing detects or serializes a second concurrent reader of the same `n`. Demonstrated directly against `TaskManager.CreateTaskTodo` (`todo.go:38` calls this same helper inside its own transaction, with **no fallback/retry at all** around the failure — unlike `upsertTagByName`, which at least retries on a name-collision-shaped transaction failure, though that retry only helps when the race is on the *same* tag name, not two concurrent creates of two different new names), firing 2 goroutines concurrently per iteration across 30 iterations on a shared-cache multi-connection in-memory sqlite database (`file::memory:?cache=shared`, `SetMaxOpenConns(4)` — this repo's own single-connection `:memory:` test convention fully serializes each `CreateTaskTodo` call's whole transaction and cannot expose this race at all). Real output: **9/30 iterations (30%) had one of the two concurrent `CreateTaskTodo` calls fail** with `CreateTaskTodo: NextCode: database table is locked: database is deadlocked (6)` — a raw SQLite driver error propagated completely unwrapped to the caller, not any of this repo's own sentinels, for an operation (creating two todos under the same parent Task) that has no legitimate reason to fail. This is the identical `gormstore/codesequence.go` shape independently found the same sweep in `mwanachama-backend-agency` (AG35), `mwanachama-backend-actor` (ACT3), and `mwanachama-backend-assetmanager` (A15) — all four repos carry their own byte-for-byte copy of this helper (no shared package), so the same fix needs applying in each. Demonstrated fact is the SQLite failure mode above; the more severe theoretical failure mode on Postgres (an unconditional `UPDATE` with no precondition letting a second writer silently proceed once the first commits, producing two rows sharing the same Code, e.g. two TaskTodos both `"TD-5"`) was not demonstrated — this repo's own tests never stand up real Postgres — and is flagged as risk, not filed as a separately confirmed finding. Fix location: `gormstore/codesequence.go`'s `NextCode` — either a CAS write (`UPDATE ... SET next_number = ? WHERE entity_type = ? AND next_number = ?`, retried on `RowsAffected == 0`) or a row lock (`SELECT ... FOR UPDATE` on Postgres). | 📋 | Found 2026-09-21 by the fleet integration-test sweep, widening ACT3/AG35/A15's finding into every sibling repo carrying the same copy-pasted `gormstore/codesequence.go`. No committed pinning test — the probe needed a non-standard shared-cache multi-connection sqlite setup (`file::memory:?cache=shared`, `SetMaxOpenConns(4)`) diverging from this repo's own single-connection test convention, and its result (a 30% failure *rate*, not a deterministic reproduction) is a probe finding, not a stable regression-guard shape; the real output quoted above is the reproduction. Probe file (`zz_probe_nextcode_race_test.go`) deleted per the sweep's own rule against leaving a probe file behind. `go build`/`go test ./...`/`go test -tags=integration ./...` all still clean (no product code touched). |
| W21 | 🐞 **BUG — `UpdateTaskTodoStatus` races `DeleteWorkflowRunArtifacts`: a concurrent rollback-cleanup soft-delete does not stop the status update's write, so the racing update's new status silently lands on an already-deleted todo row.** Same TOCTOU shape as W19/W20/`mwanachama-backend-assetmanager`'s A14, on a lead W20's own note left unchased — but on a mistaken premise: W20 assumed `TaskTodo`'s only delete path (`DeleteWorkflowRunArtifacts`) is a *hard* delete, so a racing `Updates` would silently affect 0 rows rather than succeed. Reading `workflow_run_rollback.go` directly (not the stale doc comment on `task.go`'s interface method, which still says "hard-deletes") shows the real behavior: TaskTodos anchored to a rolled-back run are **soft**-deleted (`tx.Table(m.tables.TaskTodos)...UpdateColumn("deleted", true)`), confirmed by `gormstore/tasktodo.go`'s own `TaskTodoRow.Deleted` doc comment ("marks a soft-deleted todo"). `todo.go`'s `UpdateTaskTodoStatus` reads the current row through `GetTaskTodo` (`WHERE id = ? AND deleted = false`) but writes through a bare `Where("id = ?", todoID).Updates(...)` with no `deleted` guard — identical to W19's `UpdateTask`/W20's `UpdateProject`. Driven through the real `routes.Routes(tm)` mux behind `httptest.NewServer`: `PUT /todos/{todoID}/status {"status":"completed"}` racing `DELETE /workflow-runs/{runID}/artifacts` on a todo anchored to that run, repeated across 200 fresh todos — real output: `UpdateTaskTodoStatus returned 200 concurrently with DeleteWorkflowRunArtifacts succeeding: 5/200 iterations` and `of those, the persisted todo row ended up deleted=true with the racing status update silently applied anyway: 5/5`. **Fix location**: the same CAS shape as W19/W20/A14 — `Where("id = ? AND deleted = ?", todoID, false).Updates(...)`, checking `RowsAffected` and returning `ErrTaskTodoNotFound` on 0 rows affected, in `todo.go`'s `UpdateTaskTodoStatus`. Worth fixing alongside: `task.go`'s doc comment on `DeleteWorkflowRunArtifacts` ("hard-deletes every Task and TaskTodo") is itself stale/wrong and should be corrected to say "soft-deletes" for TaskTodo, matching the actual implementation and `gormstore`'s own row-level doc comment. | 📋 | Found by the fleet integration-test sweep, re-chasing W20's own flagged-but-not-verified lead on `TaskTodo` rather than trusting its stated premise. Pinning test committed in `routes/w21_tasktodo_update_rollback_race_test.go` (`TestPinsW21_UpdateTaskTodoStatusCanWriteIntoAnAlreadyDeletedRow`) — asserts the *current* broken behavior and documents in its own comment what to flip once fixed (0 iterations written-into-deleted). `go test ./...` and `go test -tags=integration ./...` both green with it added. |
| W20 | 🐞 **BUG — `UpdateProject` races its own `DeleteProject`: a concurrent soft-delete does not stop the update's write, so the racing update's new content silently lands on an already-deleted row.** The identical shape as W19 (`UpdateTask`) and `mwanachama-backend-assetmanager`'s A14 (`UpdateAsset`/`UpdateLocation`), found while widening from those two same-session findings. `project.go`'s `UpdateProject` reads the current row through `GetProject` (`WHERE id = ? AND deleted = false`) but then writes through a bare `Where("id = ?", p.ID).Save(&row)` with **no `deleted` guard on the write at all** — so a `DeleteProject` that flips `deleted=true` in the gap between `UpdateProject`'s own read and its own write is invisible to that write, which still applies the caller's new `Name` and returns HTTP 200/nil-error. Driven through the real `routes.Routes(tm)` mux behind `httptest.NewServer`: `PUT /projects/{projectID} {"name":"raced-update"}` racing `DELETE /projects/{projectID}` on the same freshly-created project, repeated across 200 fresh projects — 193/200 iterations landed both PUT and DELETE as success, and in 186/193 of those the persisted row was confirmed `deleted=true` with `name="raced-update"` (read via a raw `SELECT`, since `GetProject`/`ListProjects` both filter the row out). Far higher reproduction rate than W19's 47/196, matching A14's own note that a bare `Save` with no extra work between read and write narrows the window less than a status-transition validation does. **Fix location**: the same CAS shape recommended for W19/A14 — `Where("id = ? AND deleted = ?", p.ID, false).Save(&row)` (or an explicit `Updates` with that same WHERE), checking `RowsAffected` and returning `ErrProjectNotFound` on 0 rows affected. Also checked and ruled out in the same pass: `Agent` has no `Update` method at all; `WorkflowRun`'s `UpdateWorkflowRunStatus` touches no `deleted` column and `WorkflowRun` has no direct delete method (only `DeleteWorkflowRunArtifacts`, an internal cleanup step, not a delete-the-run) — neither has this shape. `TaskTodo`'s `UpdateTaskTodoStatus` has the identical read-then-unguarded-write pattern, but `TaskTodo` has no direct `DeleteTaskTodo` either — its only delete path is `DeleteWorkflowRunArtifacts`'s hard-delete cascade, a weaker/different manifestation (an `Updates` against a hard-deleted row affects 0 rows silently, then the method's own trailing `GetTaskTodo` re-read surfaces `ErrTaskTodoNotFound` rather than silently succeeding) — not chased further this run, flagged as a lead. | 📋 | Found by the fleet integration-test sweep, widening W19/A14's exact TOCTOU shape (loophole catalogue #8/#2) across this repo's other soft-delete-bearing types (`Project`/`TaskTodo`/`Agent`/`WorkflowRun` — the four types W19's own log entry left as an open lead). Pinning test committed in `routes/w20_project_update_delete_race_test.go` (`TestPinsW20_UpdateProjectCanWriteIntoAnAlreadyDeletedRow`) — asserts the *current* broken behavior and documents in its own comment what to flip once fixed (0 iterations written-into-deleted). `go test ./...` and `go test -tags=integration ./...` both green with it added. |
| W19 | 🐞 **BUG — `UpdateTask` races its own `DeleteTask`: a concurrent soft-delete does not stop the update's write, so the racing update's new content silently lands on an already-deleted row.** Driven through the real `routes.Routes(tm)` mux behind `httptest.NewServer`, two real `*http.Client` requests fired concurrently per iteration — `PUT /tasks/{taskID} {"title":"raced-update",...}` racing `DELETE /tasks/{taskID}` on the same freshly-created task. `task_impl_task.go`'s `UpdateTask` reads the current row through `GetTask` (`WHERE id = ? AND deleted = false`) but then writes through a bare `Save(&row).Where("id = ?", task.ID)` with **no `deleted` guard on the write at all** — so a `DeleteTask` that flips `deleted=true` in the gap between `UpdateTask`'s own read and its own write is invisible to that write, which still applies the caller's new Title/Status and returns HTTP 200/nil-error. Repeated across 200 fresh tasks: 196/200 iterations landed both PUT and DELETE as success, and in 47/196 of those the persisted row was confirmed `deleted=true` with `title="raced-update"` (read via a raw `SELECT`, since `GetTask`/`ListTasks` would filter the row out and report nothing). **Fix location**: give `UpdateTask`'s write a compare-and-swap guard — `Where("id = ? AND deleted = ?", task.ID, false).Save(&row)` (or an explicit `Updates` with that same WHERE), checking `RowsAffected` and returning `ErrTaskNotFound` on 0 rows affected — the same shape `mwanachama-backend-assetmanager`'s A14 and `mwanachama-backend-git`'s `advanceBranchHead` already use/recommend. | 📋 | Found 2026-09-19 by the fleet integration-test sweep (loophole catalogue #8/#2: a second actor deleting while the first is mid-update; a derived/soft-delete state one write path checks and another ignores) — the identical shape found and filed the same session as `mwanachama-backend-assetmanager`'s A14 (`UpdateAsset`/`UpdateLocation` racing their own Delete counterparts). Pinning test committed in `routes/w19_task_update_delete_race_test.go` (`TestPinsW19_UpdateTaskCanWriteIntoAnAlreadyDeletedRow`) — asserts the *current* broken behavior (at least one of 200 iterations lands a racing update's content on an already-deleted row), fails loudly if the race stops reproducing, and documents in its own comment what to flip the assertion to once W19 lands (0 iterations). `go test ./...` and `go test -tags=integration ./...` both green with it added. |
| W15 | 🐞 **BUG — `IncrementFailureBudget` is documented as atomic ("atomically increments failure_pipelines_used") but is a plain read-modify-write with no CAS guard, so concurrent recovery-run increments on the same root run lose updates.** `workflow_run_failure_budget.go:48-82` reads the root run's current `FailurePipelinesUsed`/`CountedChildRunIDs` via `GetWorkflowRun`, computes `newUsed`/`newCounted` in Go, then issues an unconditional `UPDATE ... WHERE id = ?` with no `WHERE failure_pipelines_used = ?` optimistic-concurrency check and no transaction wrapping the read+write — the identical TOCTOU shape already filed as `mwanachama-backend-assetmanager`'s A13 (`CommitHold`/`ReleaseHold`) and the shape `mwanachama-backend-git`'s `advanceBranchHead` was fixed to guard against. Demonstrated through the real `routes.Routes(tm)` mux (`routes/w15_failure_budget_increment_race_test.go`, sqlite pool pinned to one connection so goroutines share the same in-memory database): created a root run, set its budget to 10, then fired 5 concurrent `POST /workflow-runs/{rootRunID}/failure-budget/increment` calls with 5 **distinct** `child_run_id`s (a legitimate, non-duplicate case — this is not testing the method's own documented idempotency-on-repeat-id behavior, which is correct). Every one of the 5 concurrent calls returned `200 used:1` — a genuinely new child, and the increment should have landed 5 times — and the run's final persisted state shows `failure_pipelines_used=1`, `counted_child_run_ids=["child-1"]`: only one of five legitimate increments survived, every run of the probe (3/3 runs, same result). This defeats the whole purpose of the counter — a mounting service dispatching several recovery pipelines concurrently under load will systematically undercount `failure_pipelines_used`, meaning `exhaustedAt`'s `used > budget` gate can stay open past the real number of pipelines actually dispatched, exactly the AI-failure-recovery runaway this repo's own CLAUDE.md flags failure-budget accounting as existing to prevent ("rebuild them as recursive CTEs... this is new transactional design, not a mechanical port"). Fix shape: a CAS-style `UPDATE ... WHERE id = ? AND failure_pipelines_used = ?` retried on `RowsAffected == 0`, or wrap the whole read-modify-write in one `db.Transaction(...)` with a row lock, matching the fix already suggested for assetmanager's A13. | 📋 | Found by the fleet integration-test sweep (loophole catalogue: concurrent-actor TOCTOU on a documented-atomic counter, same class as `mwanachama-backend-assetmanager`'s A13). Pinning test committed in `routes/w15_failure_budget_increment_race_test.go` (`TestIncrementFailureBudget_PinsConcurrentDistinctChildIDsLoseUpdates`) — asserts the *current* broken behavior (final `failure_pipelines_used` is less than the number of distinct concurrent callers) and documents in its own comment that it must go red (assert `== n`) once a CAS guard or transaction lands. `go test ./...` and `go test -tags=integration ./...` both green with this test added. |
| W14 | 🐞 **BUG — `RollbackWorkflowRun`'s documented "4-step compensation sequence" is not enforced anywhere: `DELETE /workflow-runs/{runID}/artifacts` and `PUT /workflow-runs/{runID}/status` are independently-reachable routes over its middle and final steps, so either can be called alone, in any order, on a run at any status.** `workflow_run_rollback.go`'s own doc comment states the sequence as "rolling_back → compensate cross-service artifacts → compensate own artifacts → rolled_back", but nothing outside `RollbackWorkflowRun` itself follows or enforces that order — the two primitives it calls (`DeleteWorkflowRunArtifacts`, `UpdateWorkflowRunStatus`) are each mounted as their own top-level route in `routes/workflowrun_lifecycle.go`/`routes/workflowrun.go` with no cross-check between them. Demonstrated through the real `routes.Routes(tm)` mux in two directions (`routes/w14_rollback_sequence_bypass_test.go`): **(1)** `TestWorkflowRun_DeleteArtifactsBypassesRollbackStateMachine` — a run driven to `in_progress` (never touched `rolling_back`) with one Task linked; `DELETE /workflow-runs/{runID}/artifacts` returns a clean `204` and the Task is reset to `pending` with `workflow_run_id` cleared (`GET /workflow-runs/{runID}/tasks` returns `[]`), while `GET /workflow-runs/{runID}` still reports `"status":"in_progress"` throughout — `DeleteWorkflowRunArtifacts` (`workflow_run_rollback.go:78-81`) only calls `GetWorkflowRun` to confirm the run exists, no `CanTransitionTo`/status guard at all. **(2)** `TestWorkflowRun_ForcedStatusToRolledBackSkipsArtifactCleanup` — a run with a linked Task driven `in_progress → failed → rolling_back` via three separate `PUT .../status` calls (never once calling `DeleteWorkflowRunArtifacts`), then `PUT .../status {"new_status":"rolled_back"}` succeeds (`200`, since `rolling_back → rolled_back` is a legal `WorkflowRunStatus.CanTransitionTo` transition) — the run now reports the terminal `"rolled_back"` status while the linked Task's `workflow_run_id` is still set to the run's own id, i.e. the Task was never compensated. Together these show the two halves of "rollback" can be driven fully independently: artifacts silently wiped while status lies "in_progress", or status flipped to the terminal "rolled_back" while artifacts sit untouched — either is a worse state than the sequence's own documented partial-failure path (`rollback_failed`), because neither surfaces as an error at all. Answers this sweep's specific question about atomicity: the 4-step sequence was never atomic even conceptually — steps 1/3/4 are three separate top-level DB commits by the file's own design (see its doc comment: "not part of step 3's transaction"), and this finding shows steps 3 and 4 are not even guaranteed to run together, or in order, or at all. | 📋 | Found by the fleet integration-test sweep, driving the real mux (loophole catalogue #10: "rejection paths' state-after" broadened to "ordering paths' state-after" — no single call is rejected, but the composite the sequence promises never actually happens). Two pinning tests committed in `routes/w14_rollback_sequence_bypass_test.go` asserting the *current* behavior, each documenting in its own comment what must change once fixed (most likely: `DeleteWorkflowRunArtifacts` should require `run.Status == rolling_back` before touching anything, and/or `UpdateWorkflowRunStatus` should refuse `rolling_back → rolled_back` except from inside `RollbackWorkflowRun`'s own call, or fold step 3 into step 4's write). Ruled out separately in the same investigation: `ErrForeignRunDependency` (ErrForeignRunDependency IS correctly enforced through the real `DELETE .../artifacts` route — confirmed `409` with the exact cross-run dependency message when a depended-upon run's artifacts are targeted) and `DeleteWorkflowRunArtifacts`'s own internal transaction (task resets + todo soft-deletes are atomic with *each other*, per its existing `gorm.DB.Transaction` wrapper — not in question here). `go test ./...` and `go test -tags=integration ./...` both green with these tests added. |

See [todo_done.md](todo_done.md) for W1-W10.
