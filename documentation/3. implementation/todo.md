# mwanachama-backend-taskmanager (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| ID | Pri | Status | Responsible | Task | Depends on |
|---|---|---|---|---|---|
| G2 | P2 | 📋 Not started | 🤖 agent | 🐞 **SECURITY — an agent (or, until K7/K8 land, literally anyone who can reach `mwanachama-backend-api-kazi`'s `/mcp`) forces an unbounded row scan by calling `taskmanager_traverse_relationships`/`TraverseRelationships` on a vertex with a large fan-out, returning every matching edge with no cap and no way to page.** `relationship_impl.go`'s `traverseFK` (the reverse-lookup branch, used by e.g. `assigned_to` traversed inbound) and `traverseJoin` (used by every `relFieldJoinTable` label: `blocks`/`depends_on`/`member_of`/`has_tag`) each run a bare `Pluck(...)` with no `.Limit()` — unlike every other List* method in this package (`ListAgents`, `ListProjects`, `ListTasks`, `ListTasksInProject`, `ListProjectsForTask`), which all apply `maxListPage` (500). `list_limit.go`'s own doc comment says that constant is meant to cap "every List* method reachable through the /mcp surface" — `TraverseRelationships` is reachable through that exact surface (`mwanachama-backend-api-shared`'s `mcpTraverseRelationships`, wired into `mwanachama-backend-api-kazi`'s `taskmanager_traverse_relationships` tool) and its own params struct (`taskmanagerTraverseParams`) carries no limit field for a caller to even try bounding it with. Confirmed live 2026-09-15: seeded 550 Tasks all `assigned_to` one Agent, called `TraverseRelationships(ctx, agentID, "assigned_to", DirectionInbound)` (the exact call the MCP tool wraps verbatim) and got back all 550 edges uncapped; `ListTasks()` against the same store correctly capped at 500 for contrast. Fix: apply `.Limit(maxListPage)` to both Pluck queries in `traverseFK`/`traverseJoin` (matching every sibling List* method), and consider giving `taskmanager_traverse_relationships` its own caller-supplied `limit` field the way `taskmanager_list_tasks`/`git_list_merge_requests` already have. Not filed as a self-test (case B, open hole) — filing here per the 2026-09-15 scheduled security-QA sweep (primary repo: `mwanachama-backend-api-kazi`, this finding traced back to its actual root cause in this library). Confirmed not reachable via `mwanachama-backend-api-gateway` today (that repo imports this package but never calls `TraverseRelationships` from any REST handler or its own `/mcp` copy) or `mwanachama-wakala-api` (does not import this package at all). |  |
