# mwanachama-backend-taskmanager (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| ID | Pri | Status | Responsible | Layer | Task | Raised by | Depends On |
| --- | --- | --- | --- | --- | --- | --- | --- |
| W11 | P1 | 📋 | 🤖 agent | relationship_impl.go | 🐞 **SECURITY — `TraverseRelationships` has no page cap, unlike every other List\* method in this package.** `list_limit.go`'s `maxListPage = 500` doc comment says it "caps every List\* method reachable through the /mcp surface", and it genuinely does everywhere it's supposed to — `ListProjects`/`ListAgents`/`ListTasksInProject`/`ListProjectsForTask` (`project.go`, `agent.go`) all apply it even though their own MCP tool params carry no `limit` field at all. `TraverseRelationships` (`relationship_impl.go:268`) is the one exception: its two backing helpers, `traverseFK`'s reverse-FK branch (`Pluck("id", &ids)`, line ~311) and `traverseJoin` (`Pluck(otherCol, &others)`, line ~330), run with no `.Limit(...)` clause at all — every one of the eleven `RelLabel*` edges dispatches through one of these two functions, so the gap is total, not per-label. **Measured 2026-09-14**: a throwaway test (`newTestManager`, sqlite in-memory) created one target Task and 600 "blocker" Tasks, wired each with `CreateRelationship(RelLabelBlocks, blocker→target)`, then called `TraverseRelationships(target.ID, RelLabelBlocks, DirectionInbound)` — it returned all 600 edges, not capped to 500 the way `ListTasksInProject` et al. would be for the same row count. **Threat**: `mwanachama-backend-api-kazi` exposes this verbatim as the MCP tool `taskmanager_traverse_relationships` with **zero page-size param** on either side (the MCP params struct has no `Limit` field, unlike `taskmanager_list_tasks`'s), behind an `/mcp` endpoint with no transport auth and a capability check that is a hard-coded allow-everyone stub (K7/K8, already tracked — not re-filed here) — so **any caller that can reach that port at all** can force an unbounded single-request fan-out over a busy vertex (a popular tag, a heavily-blocked task, an agent with a large assignment history), a genuine, currently-uncapped resource-exhaustion/availability lever. `mwanachama-backend-api-gateway`'s own separate, not-yet-cut-over `/mcp` copy (`internal/api/http/mcp_tools_taskmanager.go`) registers the same tool against a session/capability-gated door, so there the same query-cost risk exists for any *authorized* caller rather than anyone — narrower, but the same root cause. `mwanachama-backend-git`'s own generic graph read (`GetNeighborhood`) already has this exact cap enforced (`TestGetNeighborhood_NodeCapEnforced`) and its own `list_limit.go`/`maxListPage`, so this is not a missing pattern, just one function in this package that the pattern never reached. **Verdict**: real, reproducible gap, not filed as a self-test (would need a product-code change — adding `.Limit(maxListPage)` to both `traverseFK`'s reverse branch and `traverseJoin` — to pass, which the routine that found this is not authorized to make unprompted). Fix is small and mechanical: add `.Limit(maxListPage)` to both `Pluck` queries in `relationship_impl.go`, mirroring `project.go`'s existing `if len(edges) > maxListPage { edges = edges[:maxListPage] }` truncation style (Pluck has no `.Limit` chain point the same way `Find` does — check whichever of GORM's `.Limit()`-on-`Pluck` or a post-truncate matches this file's existing idiom before picking one). Once fixed, a follow-up sweep should convert this into a `security-*` self-test in whichever of api-kazi/wakala-api/gateway can reach it. | 🤖 security-QA scheduled sweep 2026-09-14 | — |
