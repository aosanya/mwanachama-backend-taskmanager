# mwanachama-taskmanager — completed tasks

| Task | Title | Completed | Notes |
|------|-------|-----------|-------|
| W1 | Bootstrap repo: `git init`, `go.mod`, `.gitignore`, `Makefile`, four-phase `documentation/` skeleton, `todo.md`/`todo_done.md` | 2026-09-02 | New repo (named `mwanachama-taskmanager`, not `mwanachama-tasks`, to avoid clashing with the `mwanachama-kazi` app name). Depends on [mwanachama-go-shared](../../../mwanachama-go-shared) for its entity-graph store (wired via `go.mod` local `replace`). |
| W2 | Port `task.go`'s `TaskManager` interface + `models.go` domain types (Task/Agent/Project/WorkflowRun/Deliverable/AcceptanceCriteria/Tag/TaskTodo), incl. `CanTransitionTo` state machines, unchanged | 2026-09-02 | Ported from `CodeValdWork`'s `task.go`/`models.go` into package `mwanachamataskmanager` ([models.go](../../models.go), [task.go](../../task.go)). Domain types and state machines are byte-for-byte logic, doc comments trimmed of CodeValdCortex/CodeValdAI/FEAT-ticket references that don't apply here. Two deviations from "files unchanged": (1) `Relationship`/`Direction` — not in the original `models.go` (they live in the original's `relationship.go`) but required for the `TaskManager` interface to compile — added as bare types in a new [relationship.go](../../relationship.go), with the endpoint whitelist and engine (`CreateRelationship`/`DeleteRelationship`/`TraverseRelationships` impls) deferred to W4 where they belong. (2) The concrete `taskManager` struct + `NewTaskManager` constructor from the original `task.go` are **not** ported yet — `*taskManager` can't satisfy `TaskManager` (Go's structural check) until every method has a real implementation, which doesn't happen until W8 lands; landing the constructor now would break `go build ./...` on every intermediate commit. `WorkSchemaManager`/`Publisher` type aliases are ported in their place. Also required `mwanachama-go-shared`'s S7 (`events.Publisher` — `CrossPublisher`'s replacement), which didn't exist yet; implemented it there as a small unblocking prerequisite ([events/](../../../mwanachama-go-shared/events/)) rather than stubbing a local copy. `go build ./...`, `go vet ./...`, `gofmt -l .` all clean. |
| W3 | Port `schema.go`'s `DefaultWorkSchema()` onto `mwanachama-go-shared`'s type defs | 2026-09-02 | Ported into [schema.go](../../schema.go), `types.*` → `schema.*` throughout. Dropped `PathSegment`/`EntityIDParam`/`PublishEvents` (route/topic-generation fields the new `schema.TypeDefinition` doesn't carry) and the `eventreceiver.ReceivedEventTypeDefinition("work")` entry the original appended (CodeValdCortex event-tracking plumbing, no equivalent here) — matches the pattern `mwanachama-git`'s G3 already established for the same `types.*`→`schema.*` retarget. Reset `Version`/`Tag` from the original's `4`/`"v4"` (four schema iterations accumulated over `CodeValdWork`'s history) to `1`/`"v1"` — this project's schema history starts fresh, with the original's cumulative v1-v4 properties already folded into one definition; kept `ID: "work-schema-v1"`. The `RelLabel*` edge-label constants schema.go needs (`RelLabelAssignedTo`, `RelLabelBlocks`, etc.) were pulled forward from W4 into [relationship.go](../../relationship.go) — they're pure string constants with no storage dependency, and duplicating the literals here vs. defining them once was the worse option; the endpoint-whitelist map and engine methods stay deferred to W4. Added [schema_test.go](../../schema_test.go) (`entitygraph.ValidateSchema` sanity check — dangling `ToType`, missing inverses, bad `UniqueKey` refs), mirroring `mwanachama-git`'s `schema_test.go`. `go build ./...`, `go vet ./...`, `go test ./...`, `gofmt -l .` all clean. |
| W7 | Wire `events.go`'s ~25 topics to `mwanachama-go-shared`'s `Publisher` interface | 2026-09-02 | Ported into [events.go](../../events.go) + [failure_recovery.go](../../failure_recovery.go) (the latter wasn't named in the task but its `TaskNeedsDirectionPayload`/`TaskDirectionPayload`/`RunPausedPayload`/`RunResumedPayload`/`DirectionForm` types are what events.go's own doc comments point to for 4 of the ~25 topics — porting events.go without it would leave those references dangling). All `eventbus.DomainWork + "..."` concatenations collapsed to bare string literals: `DomainWork` was already `""` platform-wide (a prior intent-keyed-topic-names rename) before this port started, so the prefix was dead weight, not a real value to preserve. Dropped `AllTopics()`/`ConsumedTopics()` entirely — both only ever fed `internal/registrar` (Cross topic registration) and the `TaskEventDispatcher`'s inbound subscriptions, neither ported here (see CLAUDE.md); `AllTopics()`'s schema-derived half also has no home — `mwanachama-go-shared`'s `schema` package dropped `TopicsFromSchema` in S2. No `Publisher`-interface "wiring" actually happens in this commit: the `publish(ctx, topic, agencyID, payload)` helper that calls it lives on `*taskManager` in the original's `task_impl_task.go`, not `events.go` — it lands with W4, and will look different besides, since `events.Publisher.Publish(ctx, topic, payload)` (S7) takes no `Event` struct/`AgencyID`, unlike the original's `eventbus.SafePublish(ctx, m.publisher, eventbus.Event{...})`. `go build ./...`, `go vet ./...`, `go test ./...`, `gofmt -l .` all clean. |
| W4 | Port straightforward impl files as-is: task CRUD, converters, project, assignment(+unblock), deliverable, relationship engine, todo, agent | 2026-09-02 | `mwanachama-go-shared`'s S3-S8 landed (Postgres `DataManager`/`SchemaManager` + a `memory` test double + a conformance suite) while W3/W7 were in flight, unblocking this. Ported into [errors.go](../../errors.go) (deferred from W2 — needed by every file here), [task_impl_converters.go](../../task_impl_converters.go), [task_impl_task.go](../../task_impl_task.go), [project.go](../../project.go), [agent.go](../../agent.go), [deliverable.go](../../deliverable.go), [todo.go](../../todo.go), [assignment.go](../../assignment.go), [assignment_unblock.go](../../assignment_unblock.go), and the relationship engine appended onto W2's [relationship.go](../../relationship.go) (endpoint whitelist, `notFoundForType`, `labelHasCreatedAt`, `CreateRelationship`/`DeleteRelationship`/`TraverseRelationships`). All mechanical `entitygraph` import-path swaps — this business logic only ever called the `DataManager` interface, never Arango directly, so it ported near-verbatim. Two real deviations: (1) the concrete `taskManager` struct now has to be declared (in `task.go`) even though `NewTaskManager` stays deferred — Go requires the receiver type to exist for every method in the package to compile, a subtlety missed in W2's "defer the whole thing" plan. (2) `publish`'s signature keeps its `agencyID` parameter (call-site compatibility + the log line) but no longer forwards it to `events.Publisher.Publish(ctx, topic, payload)` — S7's simplified contract carries no separate agency envelope, unlike the original's `eventbus.Event.AgencyID`. `go build ./...`, `go vet ./...`, `gofmt -l .` clean; still can't fully typecheck against `TaskManager` until `NewTaskManager` lands (W8). |
| W5 | Port `WorkflowRun` subsystem (create/get/list/closure, cancel+quiesce, failure-budget, watchdog) — closure/cascade queries rebuilt as recursive CTEs | 2026-09-02 | Ported into [workflow_run.go](../../workflow_run.go), [workflow_run_cancel.go](../../workflow_run_cancel.go), [workflow_run_failure_budget.go](../../workflow_run_failure_budget.go), [workflow_run_watchdog.go](../../workflow_run_watchdog.go) — near-verbatim; `GetWorkflowRunClosure`'s multi-step traversal (started_task/started_todo edges, then per-task has_todo/assigned_to/depends_on) stays as sequential `TraverseRelationships` calls rather than a hand-written recursive CTE — `mwanachama-go-shared`'s `TraverseGraph` already compiles to one recursive CTE per call (S4), so "rebuilt as recursive CTEs" turned out to already be true one layer down; no SQL to write here. `publishRunStatusEvent` and the watchdog's inline publishes now route through `task_impl_task.go`'s `m.publish` helper instead of calling `eventbus.SafePublish` directly (consistent with the rest of the package, same behavior). `go build ./...`, `go vet ./...`, `gofmt -l .` clean. |
| W6 | Port rollback (`RollbackWorkflowRun`'s 4-step compensation) with real Postgres transactions (`pgx.Tx`) — new transactional design, not a mechanical port | 2026-09-02 | Ported into [workflow_run_rollback.go](../../workflow_run_rollback.go). **The "real pgx.Tx" half of this task's goal is not met** — flagged to the user mid-task and confirmed: `entitygraph.DataManager` has no cross-call transaction primitive (no `WithTx`/`Transactor`; each method commits its own internal transaction), and this package is storage-agnostic by design — it only ever calls `DataManager`, never `database/sql`/`pgx` directly, so it has no way to wrap `DeleteWorkflowRunArtifacts`'s per-task/per-todo loop in one atomic transaction today. User's call: port the sequence exactly as the Arango original had it (sequential, best-effort — a crash mid-loop leaves partial state, same risk profile as before this port) rather than block on a new go-shared capability. Documented as a known follow-up in the file's own doc comment: closing the gap needs `mwanachama-go-shared` to grow something like `DataManager.WithTx(ctx, func(DataManager) error) error`. Coordinated with the session working on `mwanachama-api-gateway`'s routing (also touches transactional-store questions) so it isn't lost. `go build ./...`, `go vet ./...`, `gofmt -l .` clean. |
| W8 | Port `import.go` (project-bootstrap-from-JSON) | 2026-09-02 | Ported into [import.go](../../import.go) — near-verbatim (sync `ImportProject` wrapper over async `StartImportProject`/`GetImportProjectStatus`/`CancelImportProject`, in-process job-goroutine tracking, JSON doc parsing, tag upsert + `depends_on`/`has_tag` edge writes). This was the last piece needed for `*taskManager` to satisfy the full `TaskManager` interface, so `task.go`'s `NewTaskManager` constructor (declared but withheld since W2/W4) is now landed too — `go build ./...` compiling clean is itself the proof every method above resolved. Every open item on this board through W8 is now done: `go build ./...`, `go vet ./...`, `go test ./...`, `gofmt -l .` all clean across the whole package. |
| W9 | Unit tests ported/adapted; integration tests against real Postgres | 2026-09-02 | **Unit tests**: ported all 14 of `CodeValdWork`'s `*_test.go` files (`fake_test.go` harness, `schema_internal_test.go`, `task_test.go`, `agent_test.go`, `blocker_test.go`, `assignment_unblock_test.go`, `project_test.go`, `relationship_test.go`, `events_test.go`, `workflow_run_test.go`, `workflow_run_cancel_test.go`, `workflow_run_failure_budget_test.go`, `workflow_run_id_test.go`, `workflow_run_rollback_test.go`) — 162 test functions, all passing incl. `go test -race`. Kept the original's own lightweight `fakeDataManager` (adapted, not `mwanachama-go-shared`'s schema-aware `memory.Backend`) — the memory package's `UpsertEntity` requires an active published schema via `GetActive`, which would have forced schema-seeding boilerplate onto ~150 call sites that the original fake never needed; not worth the churn for what's still a faithful port. Real adaptation (not just import-path swaps) needed wherever tests touched the publish boundary: `recordingPublisher`/`publishedEvent` (in `fake_test.go`) drop the `AgencyID` field entirely and `events` is now a topic-only projection — `events.Publisher.Publish(ctx, topic, payload)` (S7) carries no agency envelope, unlike the original's `eventbus.Event`, so every `"topic\|agencyID"` string assertion became bare `"topic"` and `ev.AgencyID`/`ev.Timestamp` assertions were dropped (S7's contract stamps neither). Dropped `TestAllTopics_StableSurface` — `AllTopics()` isn't ported (W7). **Postgres integration tests**: new [postgres_integration_test.go](../../postgres_integration_test.go), 5 tests covering task CRUD, agent upsert (proving Postgres `UpsertEntity`'s `ON CONFLICT` unique-key path), assign+traverse (proving the recursive-CTE `TraverseGraph`), workflow-run create/closure/rollback, and full `ImportProject`. Mirrors `mwanachama-go-shared`'s own `postgres/backend_test.go` pattern exactly: skip via `POSTGRES_URL` env var (no build tag — the Makefile's `test-pg: go test -tags=integration ./...` predates that repo's actual implementation and was never updated there either; left as-is here for consistency, the tag is simply unused), open with `postgres.Open`, stand up scratch `workit_`-prefixed tables via `postgres.DDL`/`DropDDL`, seed+activate `DefaultWorkSchema` via `SetSchema`/`Publish`/`Activate`. **Not run against a live Postgres this session** — no database available; the go-shared session hit the same limit for its own conformance suite (declined a docker-based attempt). `go build ./...`, `go vet ./...`, `go test ./...`, `gofmt -l .` all clean; Postgres tests correctly skip with a clear message when unset. |

## Archived board context

`mwanachama-taskmanager` — Go library, module
`github.com/aosanya/mwanachama-taskmanager`. Consumed by
[mwanachama-api-gateway](../../../mwanachama-api-gateway) for
[mwanachama-kazi](../../../mwanachama-kazi).

### Why this repo exists

`mwanachama-kazi` needs task/workflow management. `CodeValdWork` already
built this — as an ArangoDB-backed library for CodeValdCortex agencies, with
a gRPC service and an event-dispatcher process driving AI-failure-recovery
orchestration. Neither fits `mwanachama-api-gateway`, which is Postgres-only
and runs as one service with no sub-services.

**Decision: full port, same public API surface.** Exploration found
`CodeValdWork`'s business logic (`task*.go`, `workflow_run*.go`) is entirely
storage-agnostic Go calling `entitygraph.DataManager` — the only real new
engineering is the `WorkflowRun` rollback/cascade subsystem, which needs
proper Postgres transactions where the Arango original had none. So the
port is: reuse the `TaskManager` interface/domain types/state machines close
to verbatim, retarget storage onto
[mwanachama-go-shared](../../../mwanachama-go-shared)'s Postgres entity-graph
engine, and drop gRPC/proto/cmd/registrar/the event-dispatcher process.

Full task breakdown and rationale: originally scoped in
`/Users/tony/.claude/plans/kind-snacking-rose.md`.
